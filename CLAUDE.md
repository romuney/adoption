# CLAUDE.md

Это рабочая папка BI-проекта «Proteus Adoption» — перестройка боевого дашборда
13896 внутри корпоративного BI **Proteus** (форк Superset: eCharts-виджеты с
кастомным JS, viz_type `react_sanbbox`). Это не приложение и до передачи не
было git-репозиторием: здесь живут макет, виджеты, генераторы SQL и стенды, а
боевая система правится владельцем руками в UI Proteus по инструкциям из папок
поставки.

**Начни с `CONTEXT.md`** — полный контекст проекта на 2026-09-22: живой статус,
архитектура, вердикты, грабли платформы и ClickHouse, правила владельца,
открытые хвосты. Здесь — только навигация и команды.

## Куда идти (быстрая навигация)

| Пришла задача | Куда идти |
|---|---|
| «Что это за проект, что уже сделано, что нельзя делать» | `CONTEXT.md`, затем `memory/` |
| «Вставь SQL в датасет / создай датасет» | последняя папка `Обновление <дата> (…)`, файл `1. SQL…` |
| «Замени JS чарта / вставь в чарт NNN» | та же папка поставки, файл `N. JS…` |
| «Где текущий прод-файл виджета?» | `Виджеты/<имя>.chart.js` (+ `.data.sql`, `.NOTES.md`) |
| «Сверить вёрстку/поведение с эталоном» | `Макет/` (index.html + assets/), дампы зон — `Виджеты/<имя>.html` |
| «Что за объект в бой. Proteus?» | `Proteus дашборд/` (YAML-экспорт: датасеты, чарты, борд 13896) |
| «Как устроен ответ куба / имена полей?» | `Виджеты/FIELDS.md` |
| «Править шаблон куба» | корень: `.gen_cube6.py` (генератор v6) → `.pa6_*.sql`; v5.1 — `.gen_cube5.py` → `.pa5_*.sql` (откатная копия) |
| «Прогнать виджет без браузера» | `node .render/vm*.js` (моки — `Виджеты/*.mock.json`) |
| «Проверить джиню шаблона» | python3 + jinja2: `.jt6.py` (куб v6) / `.jt_who.py` (who) / `.jt_aud.py` (аудитория); v5.1 — `.jt5.py` |

## Карта папок

| Путь | Что это | Статус |
|---|---|---|
| `Макет/` | Интерактивный HTML-макет: `index.html` + `assets/` (app.css, app.js, ui.js, charts.js, data.js — синтетика) + `vendor/echarts`; `Архитектура 2.5.md` — source of truth раскладки | **Неизменяемый эталон вёрстки** — правки идут в JS-виджет, не в макет |
| `Виджеты/` | Продакшн-виджеты: `pa-reports-body.*` (тело, чарт 788805), `pa-cohorts.*`, `pa-who.*` («Кто смотрит» ⇄ «Динамика»), `pa-strip.*` (полоска настроений/настроек), `pa-shelf.*` (полка фильтров — снята с листа 2.5), `pa-audience.*` (панель «Аудитория») + `FIELDS.md`, `SELF_CHECK.md`, `*.NOTES.md` (рабочая память по виджету), `*.mock.json` (моки vm-стендов), `*.html` (эталонные дампы зон макета) | Единственные редактируемые прод-файлы |
| `Proteus дашборд/` | YAML-экспорт боевых объектов Proteus: датасеты, чарты, дашборд 13896, cssTemplates | Справочник, read-only |
| `Обновление YYYY-MM-DD (…)` | Папки поставки владельцу: `0. Инструкция.md` + нумерованные SQL/JS-файлы | Действуют: 2026-09-21 (v6 первый лист 2.5, ждёт применения) и 2026-09-19 ×2 (в бою; полку — удалить после применения v6) |
| `.render/` | Стенды: `vm*.js` (прогон JS виджетов в Node vm на моках), `check.js` (геометрия через CDP), `start-browser.sh` (CDP на 9333), `board-stand.html`, `_dump_*.js` (снимают эталонные дампы), `screenshot.js`/`shot-state.js`, `measure-live.js` | Служебное; `node_modules` в репо нет — `npm install` в `.render/` при надобности CDP-скриптов |
| `skills/` | Скилл **proteus-echarts-builder** (обязателен для любой работы с `.chart.js`) и `proteus` (MCP-обёртка) | Преемнику: поставить proteus-echarts-builder в свой `~/.claude/skills/` или читать как документацию |
| `memory/` | Полные тексты памяти исходного агента (19 файлов) | Источник деталей; выжимка — `CONTEXT.md` |
| `DESIGN_SYSTEM.md` | Дизайн-система TeamPulse (токены, компоненты) для HTML-отчётов | Справочник |
| `Helicopter.yaml` | Конфиг helicopter-ноды: свежесть GP-таблиц и gp-параграфы предагрегатов `prod_proteus.pa_*` | Справочник |
| Корень | Живые инструменты генерации/проверки (белый список ниже) | Скратч в корне не копится |

## Корневые инструменты (белый список)

| Файл | Роль |
|---|---|
| `.gen_cube6.py` | генератор куба v6 (тело+когорты, людская шина who, state_j) → `.pa6_*.sql` + дефолт-эталоны |
| `.jt6.py` | jinja-стенд куба v6 (сверка рендера шаблона с эталоном генератора) |
| `.gen_cube5.py` / `.jt5.py` | то же для v5.1 (откатная копия, действует в бою до применения v6) |
| `.gen_who.py` / `.jt_who.py` | датасет pa_who (list/ctx/freq/ts/total, 10 плеч UNION; check_arms считает колонки плеч) |
| `.jt_aud.py` / `.rend_aud.py` | jinja-стенд и дефолт-рендер панели Аудитория (`.pa_aud.sql`) |
| `.pa6_*.sql` / `.pa5_*.sql` | шаблоны кубов (правятся ТОЛЬКО генераторами) + `*_default.sql` — эталоны для SQL Lab |
| `.pa_dicts.sql`, `.pa_who*.sql`, `.pa_aud*.sql` | SQL остальных датасетов |
| `.serve_saved.py` | локальный сервер (:8924) сохранённых страниц Proteus для замеров шва (`measure-live.js`) |
| `.pmcp.sh` | вызов инструмента Proteus MCP из шелла (нужен корпоративный `dp` CLI; содержит внутренний URL шлюза) |

## Команды

```zsh
node Макет/_test.js                        # логика всех состояний макета (браузер не нужен)
node --check Виджеты/pa-reports-body.chart.js
python3 .gen_cube6.py                      # регенерация SQL куба v6: .pa6_*.sql + эталоны
python3 .jt6.py                            # jinja-стенд v6 (нужен python3 + jinja2)
node .render/vm5.js                        # vm-прогон тела+когорт на моках
node .render/vm_who.js                     # vm-прогон «Кто смотрит» (также vm_strip/vm_shelf/vm_aud)
node .render/check.js <абс-путь.html> [width]   # геометрия (нужен CDP-браузер)
```

Окружение: jinja-стендам нужен python3.9+ и `pip install jinja2` (исходный
`.venv` в репо не входит); CDP-скриптам — `npm install` в `.render/` и браузер
на 127.0.0.1:9333 (`.render/start-browser.sh`; порт 9222 закрыт песочницей
исходной машины — подробности в `memory/proteus-adoption-render-sandbox.md`).
Живой Proteus — только через MCP-скилл `proteus` (корпоративный доступ) или
руками владельца в UI.

## Конвенции

- Правки боевого Proteus вносит владелец вручную: любая доставка — папка
  `Обновление <дата> (<версия>): <суть>` с `0. Инструкция.md` и нумерованными
  файлами; в инструкции — шаги с проверками и стоп-точками («красное →
  прислать текст ошибки целиком»).
- Именование: точка-префикс в корне = живой инструмент из белого списка;
  `_`-префикс = служебное; цифры «1. 2. 3.» = файлы поставки.
- **Скратч не копить**: дампы, пробы, тестовые проходы удаляются сразу по
  закрытии этапа; опыт фиксируется финальным правилом (в CLAUDE.md / память),
  не историей итераций.
- Типографика: потолок веса 700; минус типографский; разряды тонким пробелом.
- `Виджеты/*.NOTES.md` — точка восстановления после обрыва сессии: прочитать
  целиком, найти первый пункт плана ≠ DONE и продолжать с него.
- SQL-шаблоны куба правятся ТОЛЬКО генераторами (`.gen_*.py`), не руками;
  после правки — прогон jinja-стенда, перед боем — прогон дефолт-рендера в
  SQL Lab владельцем (стенды не исполняют SQL, см. CONTEXT.md §9).
- Существенные факты фиксировать в долгой памяти, не только в чате.
