---
name: proteus-adoption-project
description: "Цель проекта «Proteus Adoption» — полный рефакторинг дашборда вайбкодингом; дорожная карта, артефакты, статус"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4eef0bb7-7e46-4c7b-bea1-0e4319e8b76b
  modified: 2026-09-21T16:14:16.929Z
---

# Проект: рефакторинг дашборда Proteus Adoption

Цель: полностью отрефакторить продакшн-дашборд «Proteus Adoption» (Proteus/Superset BI, id 13896) с помощью Claude (вайбкодинг).

## Конвейер данных (общая схема)
Helicopter-нода (id 844437) → Greenplum-стейджинг → витрина `usr_cross_data.proteus_views_1` → выгрузка в ClickHouse `prod_proteus.proteus_adoption` + словари → датасеты Superset (БД «Proteus CROSS») → дашборд.

## Дорожная карта (согласована с пользователем)
1. ✅ Разбор конфигов: `Helicopter.yaml` + экспорт дашборда (папка «Proteus дашборд/») — прочитаны 2026-09-14.
2. ✅ Аудит данных: что лежит в витрине, что используется, что не используется — [[proteus-adoption-audit-opportunities]].
3. ✅ Макет нового дашборда — v2 от 2026-09-15 (переработан по фидбеку владельца), лежит в `Макет/`. Подробности: [[proteus-adoption-mockup]].
4. ⬜ Рефакторинг ноды Helicopter и самого дашборда (после утверждения макета). Вкладка «Отчёты» макета требует доработки ноды: когорты по dashboard_id (сейчас retention-датасет без него).

## Артефакты в рабочей папке
- `Helicopter.yaml` — конфиг ноды (paragraphs: gp-скрипты, python_condition).
- `Proteus дашборд/` — YAML-экспорт Superset: dashboards/, charts/ (13 шт.), datasets/ (11 шт.), databases/, cssTemplates/, metadata.yaml.

## Ключевые факты
- Дашборд: slug `proteus-adoption`, uuid 40cc0ef4-f7ed-4183-aa59-34551f398eee, версия layout v2, refresh_frequency 3600, ссылка на ноду helicopter.tcsbank.ru/notes/844437.
- Доступ через MCP-скилл `proteus` (dp ai mcp remote) — можно смотреть/менять борд и SQL графиков программно.
- В ноде есть большой кусок мёртвого legacy-кода — см. [[proteus-adoption-pipeline]].
- Обновление кеша борда в конце ноды — `refresh_dashboard()` под учёткой из env (Z_ENV_CREDENTIAL_*), slug + permalink.

## Журнал действий
- 2026-09-14: прочитаны Helicopter.yaml и весь экспорт дашборда; написаны память-файлы: pipeline, datamodel, dashboard-current, audit-opportunities. Вопросы к пользователю перед макетированием заданы (см. переписку): аудитория/назначение борда, границы рефакторинга ноды.
- 2026-09-14: собран интерактивный HTML-макет в `Макет/` (4 вкладки, ECharts, синтетические данные в структуре будущих SQL-датасетов). Логика прогнана `node Макет/_test.js` — зелено. Вёрстка глазами НЕ проверена: headless Chrome в песочнице не отдаёт скриншот/DOM. Детали и отступления от ДС → [[proteus-adoption-mockup]].
- 2026-09-14: получены ответы по аудитории и флоу → [[proteus-adoption-requirements]]. Вкладка «Анализ целевой аудитории» обязательна; нужен per-report анализ аудитории; AD-группы как прокси — с флагом «широких» групп (Tableau All и т.п.). Подтверждена осуществимость per-report покрытия на текущем CH (allowed_logins + ad_groups_dash × mdm_employee_daily_proteus).
- 2026-09-15: макет переработан по фидбеку владельца (вкладка «Отчёты» = обзор+каталог с подшапкой разрезов, когорты как в бою, подписи на каждом столбике, покрытие компании убрано, парето убрано). Геометрия проверена CDP на 1280–1920, числа сверены по DOM. Скриншоты `.render/shots/`.
- 2026-09-15 (вечер): второй фидбек — раскраска когорт возвращена к дивергентной от медианы (логика и палитра #ffda7a/#fff/#a0deff вытащены из YAML боевого графика 711333), таблицам возвращены боковые отступы, шапки когорт однострочные, плашка ЦА разбита на подписанные блоки «источник доступа» + «Отчёт», сплит аудитории фиксированной высоты со скроллом, «Реактивированные»→«Вернувшиеся», старые «Вернувшиеся»→«Продолжающие». Тесты и геометрия 1280–1920 зелёные. Подробности в [[proteus-adoption-mockup]].
- 2026-09-16: зарегистрирован датасет-куб pa_cube_v1 (169011, jinja-параметры период/режим, рендеры 4-7с) + словари параметров 169010/169009. Замеры объёмов факта. Подробности: [[proteus-adoption-cube-dataset]].
- 2026-09-17: коррекция владельца — нативных фильтров на борде НЕ будет, полка фильтров = кастомный ECharts-чарт (кросс-фильтры к телу). Дорожная карта пересобрана: сначала платформенная проверка «долетает ли applyCrossFilter до filter_values() в jinja», затем S1 pa_dicts → полка-чарт → тело-чарт.
- 2026-09-17 (продолжение): пункт 1 ЗАКРЫТ — владелец дал прецедентный борд 59922 (чарты 783469/783708, react_sanbbox, датасет 168383 с filter_values('grp') в SQL): applyCrossFilter из JS доезжает до jinja. Грабли эмиттов и API виджетов → [[proteus-adoption-echarts-platform]]. Команда владельца: адаптировать макет скиллом proteus-echarts-builder. Сделано: bootstrap болванок Виджеты/pa-reports-body.*, NOTES §0–§4/§6 заполнены (макет = SPA, реестр из памяти v3 + модулей; хост react_sanbbox ⇒ ECharts-серии макета перерисовываются своим SVG; трансляция const/стрелок в контракт). Ждём «да» на план сборки. Макет реально v3 (2026-09-16): 2 вкладки, накопительный выбор каталога, конструктор ЦА.
- 2026-09-18: тело собрано и сдано (чарт 788805, куб v3→v4→v5 «все разрезы одним ответом», закрепляемость отделена в чарт pa_cohorts на датасете pa_coh_v4 169589; режим самовлияния тела выключен владельцем — смена вкладок клиентская). Пак для вставки: «Обновление 2026-09-18 (v5 все разрезы сразу)/». Вечером — визуальный раунд 7 по скринам живого борда: типографика SVG 1:1 (без масштабирования шрифтов), радиус KPI, починен тумблер «подробнее», сохранение скролла при кликах, nowrap подзаголовков, radius 4 канвы. Грабли DOM-рендера → [[proteus-adoption-echarts-platform]]. Проверки: validate 0 FAIL оба чарта, smoke код 2 (песочница), функциональные тесты на CDP-стенде зелёные; живой борд ждёт перепастки JS.
- 2026-09-18/19 (ночь): ЭПОПЕЯ С ШВОМ ЗАКРЫТА — побеждена на живом борде 60260 (владелец: «УРА сработало»). Раунды 8-11 не могли убрать разрыв: гасили паддинг .chart-container, а реальный источник — padding:16px у `.dashboard-component-chart-holder` (emotion-правило форка, в чанках невидимое) + отсутствие display:flex у .chart-slice + display:inline у iframe. Финал: вставка раунд 11 + одна строка `padding:0 !important` у холдера (раунд 12), всё в «0. Инструкция.md». Живой шов 48px → 16px = внутренним желобам виджетов. Методология на будущее: геометрию борда проверять ТОЛЬКО репликой сохранёнки (.serve_saved.py 8924 + .render/measure-live.js) или живым консольным замером; самодельный стенд board-stand.html вводил в заблуждение (приписал платформе flex у chart-slice). Полный разбор → [[proteus-adoption-html-embedding]].
- 2026-09-18 (вечер, след. этап): СТАРТ ПОЛКИ ФИЛЬТРОВ — виджет `Виджеты/pa-shelf.*` (bootstrap скиллом, Режим A). Объём согласован с владельцем: «Полная полка + куб» = все контролы сайдбара макета КРОМЕ текстовых поисков. Ключевые решения (NOTES pa-shelf §3/§4): эмит-колонки period_param/rep_f/coll_f/own_f/auth_f/pub_f/act_f/cert_f/lvl3_f/lvl4_f/stream_f/spec_f/adg_f/heads_f/exc_f (все IN, носители НЕ в SELECT приёмников); дефолты published/actual/excludeOwners = ВКЛ зашиты в джиню куба (цифры входа ИЗМЕ НЯТСЯ — проговорить владельцу); свитки эмитятся только при отличии от дефолта, сброс = applyCrossFilter([]); поиск по названию отчёта → мультиселект по справочнику (отклонение от макета); логин/ФИО отрезаны (IN-only, 46К логинов, ФИО нет); НОВЫЙ датасет pa_dicts (section/k/nm/parent, ~23К строк) создаёт владелец; скоупы: полка→[тело,когорты] без себя, полку исключить ИЗ скоупа тела (иначе клики тела перезапрашивают справочник). Эталон вёрстки: Виджеты/pa-shelf.html = рендер sideNav макета (.render/_dump_shelf.js). Дальше: план в NOTES §0 этапы 3-6 (SQL-генератор → JS полки → сдача → поставка).
- 2026-09-19: ПОЛКА ЗАКОНЧЕНА И ПОСТАВЛЕНА. Сборка pa-shelf.chart.js блоками 1-7 (SELF_CHECK 68/69 PASS, WARN Z4 — 2 осознанных хвоста; smoke код 2, вместо браузера vm-стенд .render/vm_shelf.js 39/39). SQL-этап закрыт раньше (генератор v5.1 .gen_cube5.py + .pa_dicts.sql, стенд .jt5.py 36/36). Поставка: «Обновление 2026-09-19 (полка фильтров + куб v5.1)/» — 0-инструкция (SQL Lab → pa_dicts → замена SQL 169602/169589 → чарт полки лимит 30К → борд → скоупы), 1/1a/2/2a/3 SQL + 4 JS + 5 FIELDS (секция полки дописана). Ключевой проговор в инструкции: дефолтные цифры тела/когорт УМЕНЬШАТСЯ против v5 (дефолты pub/act/exc='1' в джине = включённые свитки макета). Дорожная карта: S1 (pa_dicts) и полка закрыты; остался S3 «Аудитория». Детали → [[proteus-adoption-cube-dataset]].
- 2026-09-19 (вечер): СМЕНА НАПРАВЛЕНИЯ по живому фидбеку владельца (полка вставлена, применение фильтров 5–6 с — тяжело). Решено: полка-сайдбар убирается → тонкая полоска настроек (период+свитки+люди) мини-чартом над телом; каталог = основной фильтр; вкладка «Аудитория» внутри тела (S3) закрывает кейс 3 двунаправленной связкой (отчёт→зрители по УС, люди→топ-отчёты) отдельным лёгким датасетом от того же эмита выбора. ПРЕДАГРЕГАТ: владелец добавил gp-параграфы в Helicopter (из proteus_views_1) + gp_to_click → prod_proteus.pa_evd_day / pa_emp_attrs / pa_dash_meta — «уже на клике». Готов куб v6-прототип (.pa6_body.sql, форма 36 колонок + та же джиня) и замерка .pa6_p1…p5_*.sql (DESCRIBE/счётчики/топ-lvl3, дефолт d, фильтр lvl3) — владелец прогоняет в SQL Lab, ждём цифры (цель ≤2 с). Дальше: генератор+стенды v6, полоска настроек, вкладка Аудитория.
- 2026-09-19 (ночь): ПАНЕЛЬ «АУДИТОРИЯ» СОБРАНА И СДАНА — `Виджеты/pa-audience.*` (Режим A, пятый виджет). Форма: ПАНЕЛЬ-приёмник эмита тела (mode_param+sel_f), не вкладка — куб тела sel-маску не читает (самовлияние выключено). Ядро = head + 5 KPI + наблюдения + «Охват по разрезу» (sub-tabs УС-3/УС-4/Спец/Стрим/AD/Тим-лиды/+Отчёты) + btable с барами и ИТОГО; клик строки → эмит cut:*, клик отчёта топа → смена области; adg-строки без клика (CUTS когорт не знает). Датасет pa_aud_v1 (12 колонок, ~до 400 строк) на предагрегатах pa_evd_day×pa_emp_attrs×pa_dash_meta. Сдача: validate 67/68 (WARN Z4 — 5 хвостов §6 NOTES: воронка прав, поимённый список, конструктор ЦА, когорты cut:adg, широкая аудитория), smoke код 2 (SEGV headless-shell) → vm-стенд .render/vm_aud.js 22/22. FIELDS.md дописан секцией виджета 4 + чек-лист «после ревью». Грабли сдачи: (1) забытый маркер [ЗАПОЛНИ] в комментарии БЛОКА 6 (M4b); (2) Z1b ловит ЛЮБУЮ пару «цифра–тире–цифра» в §2 — проза «renderAudience 1420–1725» распозналась диапазоном чтения; лечится правкой прозы, номеров строк в §2 не писать. Ждёт ревью владельца; папка Обновление — после.
- 2026-09-19 (вечер, продолжение): ПОСТАВКА «АУДИТОРИИ» СОБРАНА — «Обновление 2026-09-19 (панель Аудитория)/»: 0-инструкция (SQL Lab 1a → датасет pa_aud_v1 + 12 измерений → чарт eCharts лимит 1000 → борд 60260 под телом → скоупы) + 1 SQL (джиня .pa_aud.sql) + 1a дефолт-рендер (скратч .rend_aud.py, SandboxedEnvironment как в .jt5) + 2 JS + 3 FIELDS. Ключевое в скоупах: тело 788805 получает ПАНЕЛЬ в приёмники (первое изменение скоупа тела с v5 — полку туда не клали из-за 23К, панель лёгкая ~400 строк); панель эмитит только в когорты; полка → +панель пока стоит. Грабля процесса: Виджеты/pa-audience.data.sql оставался БОЛВАНКОЙ bootstrap при этапе «SQL собран DONE» — реальный SQL жил в скратче .pa_aud.sql; data.sql синхронизирован только при сборке поставки. Урок: validate не смотрит содержимое data.sql, сверять его с скратчем при закрытии SQL-этапа. Клики: строка панели → cut:* в когорты; отчёт из «Отчётов области» (секция есть только при cut-области, гейт have+iscut) → report в когорты; панель и тело себя не трогают.
- 2026-09-19 (поздний вечер): МАКЕТ 2.0 СОБРАН — «Макет/» переписан под один экран без вкладок и полки: каталог-руль слева (подшапка 5 разрезов, накопительный выбор), полоска настроек сверху (период + поповер опций + чипы + сброс), «Кто смотрит» панелью на всю ширину (разрезы ТОЛЬКО lvl4/stream/adg/heads), заглушки воронки прав (этап 2) и поимённого списка (этап 3), когорты+частота внизу. ДВЕ ШИНЫ кросс-фильтров: отчётная (эмит mode_param+sel_f) и людская (клики «Кто смотрит»/людских вкладок каталога → люди-фильтры куба ⟳, каталог сужается). ПРАВИЛО БЕЗ ДУБЛЕЙ (прямое требование владельца по ходу работы): каждый список один раз — панель «Что смотрит публика» УДАЛЕНА, УС-3/спец убраны из «Кто смотрит». Вводная про раскладку: владелец может делать вкладки борда и несколько чартов-приёмников справа — правило зафиксировано в «Макет/Архитектура 2.0.md» (в один чарт — клиентские смены разрезов; отдельными — слушатели эмита). _test.js переписан, ЗЕЛЁНЫЙ (людская шина сужает каталог 37→23, поповер не глотает клики чипов — фикс acted, MAU=столбику). index.html + howModal обновлены. Прежний v3 — бэкап .макет-v3/. Дальше: ревью владельцем макета глазами (браузер), затем пересборка полоски/тела под 2.0 и куб v6 (замерка .pa6_probe.sql у владельца на прогоне).
- 2026-09-20: МАКЕТ 2.1 — ВОЗВРАТ К ДВУМ ВКЛАДКАМ по фидбеку владельца (посмотрел 2.0 глазами: «первая версия с двумя вкладками лучше»). «Отчёты»: каталог только с отчётными разрезами (отчёты/коллекции/владельцы), правая панель ПЕРЕКЛЮЧАТЕЛЬ Динамика⇄«Кто смотрит» (решение владельца из AskUserQuestion), сводная «кто смотрит» = людские разрезы (УС-3/УС-4/спец/стрим/AD/тим-лиды) + режим «Поимённо» + корзины частоты ВНУТРИ панели; когорты полноширинно внизу. «Аудитория»: возврат v3-механик (конструктор ЦА, воронка с ступенью «есть доступ», динамика охвата, segstrip-поимённо с «не заходили», охват по разрезам), область наследует каталог (пикер области удалён — правило без дублей). v3-код ВОССТАНОВЛЕН ИЗ ТРАНСКРИПТА прошлой сессии (бэкапа на диске не было): python-скрипт собрал app.js 2453 строки из чанков Read по tool_use_id → Макет/_app_v3_backup.js. Проверки: _test.js зелёный, check.js 1920/1280 чисто, скриншоты ГЛАЗАМИ (_shots/21/, 4 кадра — воронка, segstrip, поимённо, переключатель все на месте). Грабли 2.1: (1) rightPanel вычислялся ВНЕ zone() → chart() получал id ch-z-0 и изоляция зон ломалась (тест поймал «смена ctbase пересобрала график»); лечится ленивым () => внутри zone. (2) В стабе DOM funnelSvg(w=undefined) даёт пустую строку — ступени воронки проверять прямым вызовом. (3) U.panel без body даёт 'undefined' в panel-b. Док: «Макет/Архитектура 2.1.md». Дальше: ревью владельца, затем куб v6 (замерка у владельца) и пересборка тела/полоски под 2.1.
- 2026-09-19 (ночь, ревизия): БОЛЬШАЯ ЧИСТКА ПАПКИ по просьбе владельца («миллион файлов, не ориентируюсь»); в ходе неё владелец дал постоянное правило — тестовые артефакты не храним, удаляем сразу, опыт фиксируем финальным правилом в CLAUDE.md, не по итерациям ([[feedback-no-test-artifacts]]). Удалено из корня 396 файлов мёртвого скратча: дампы MCP-ответов (.q_*/.b*/.jt_*.json…), генераторы v1–v4 (.gen_cube…4.py, .gc4_*, .jt4.py), пробы pa3/pa4, разовые скрипты миграций (.split_coh, .refactor32, .mk*, .body_before_split.js), HTML-дамп 3МБ, .DS_Store. Живое оставлено белым списком (таблица в CLAUDE.md): .pmcp.sh, .gen_cube5.py, .jt5.py, .jt_aud.py, .rend_aud.py, .serve_saved.py, .pa5_*/.pa_dicts/.pa_aud_*/.pa6_* SQL, .mcp.json. .render: удалены разовые пробы (zoom/vm-check/states/probe*/coh-*/eval, профили браузеров, mock-data.js), tmp_*.tsx → .html_css/. По правилу владельца удалены и применённые поставки: «Обновление 2026-09-17», «(куб v3)», «2026-09-18 (v4…)» и «2026-09-18 (v5 все разрезы сразу)» (перекрыта v5.1 от 09-19; снапшотов TM нет, восстановление не требуется по правилу). Остались только действующие поставки 2026-09-19. CLAUDE.md переписан: раздел «Куда идти» (навигация по типовым задачам), таблица корневых инструментов (роли/чтение→запись), команды .jt4→.jt5 + .jt_aud, конвенции «скратч не копить» и «опыт — финальным правилом». Проверки после чистки: .jt5.py 36/36, .jt_aud.py 0 FAIL, node --check виджетов OK. Итог: в Finder корень = 3 файла + 8 папок, инструменты скрыты точкой.
- 2026-09-21: МАКЕТ 2.2 по устному фидбеку владельца (смотрел 2.1) — «вкладки симметричны, у аудитории свой выбор отчётов». Сделано: (1) фикс баги — из «Кто смотрит» нельзя было вернуться в «Динамику»: переключатель теперь в шапке панели В ОБОИХ состояниях; (2) поимённый режим «Кто смотрит» получил ГРУППИРОВКУ по людским разрезам (дропдаун, секции со счётчиком/долей, сворачивание кликом, adgroup — во все свои группы); (3) вкладка «Аудитория» перестроена: СВОЙ каталог слева (audPicks независим от picks «Отчётов», data-catscope, чипы полоски от активной вкладки), KPI 6 карточек с новой метрикой **Adoption за год** (доля ЦА зашедших; мок: окно 12 мес через visitorsOf('m'), прод: с 1 января), scopebar сжат до колонки ЦА, воронка рядом с каталогом, динамика охвата полноширинно, «Кто из ЦА» переведён в форму «Кто смотрит» (дропдаун 8 разрезов ⇄ Поимённо+segstrip); «Охват по разрезу» как отдельная панель и кнопка «→ к каталогу» удалены. Правило без дублей смягчено до «в рамках вкладки». Грабли: aside-зона сплита обязана НЕ быть display:contents (иначе панели падают в grid-ячейки); колонке каталога нужен minmax(533px) под 5-колоночную таблицу на 1280. Проверки: _test.js зелёный (12 блоков), check.js 1920/1280 чисто в 12 состояниях (список расширен группировкой и каталогом области), скриншоты глазами _shots/22/ (4 кадра), док → «Архитектура 2.2.md» (старое имя удалено), _shots/21 удалены по правилу чистки. Дальше: ревью владельца 2.2 → пересборка полоски/тела под 2.2 и куб v6 (замерка у владельца).
- 2026-09-21 (второй раунд): МАКЕТ 2.3 — доводка ПЕРВОЙ вкладки по устному фидбеку владельца («добьём вначале первую вкладку»). Сделано: подшапка каталога (отчёты/коллекции/владельцы) прижата к левому краю, не на всю ширину; внятный отступ между правой панелью и закрепляемостью; легенда динамики = КЛИКАБЕЛЬНЫЕ ПИЛЮЛЬКИ над графиком (клик гасит/возвращает серию, тултип на пилюльке, текстовая легенда под графиком убрана); «Кто смотрит» = поимённый список ЕДИНСТВЕННЫМ режимом (сводная по разрезам удалена): сигаретка частоты сверху (клик сужает список), тулбар СЛЕВА с дропдауном «Группировка списка» + счётчиком «N из M», дерево групп с кареткой сворачивания (клик по группе = людская шина, сотрудники строками с отступом), строка человека = ФИО + бейдж «рук.» + логин / подразделение+спец / стаж / активных дней / визит / сегмент-чип; высота панели как у каталога (первые 60 строк, в бою серверная пагинация). Сессия оборвалась на лимите 32МБ при чтении скринов; восстановлено следом: кадры 03/04 проверены глазами (числа сходятся с DOM-пробой: корзины 475/283/370/524/116 = «60 из 1 768»), док → «Макет/Архитектура 2.3.md» (2.2 удалён), скрины _shots/23 (4 кадра), _test.js зелёный, check.js 13 состояний чисто на 1920/1280. Грабля скринов: captureBeyondViewport:true в headless отдаёт устаревший кадр — лечится высокой вкладкой + false ([[proteus-adoption-render-sandbox]]). Дальше: ревью владельца 2.3 → пересборка полоски/тела под 2.x и куб v6 (замерка у владельца).
- 2026-09-21 (вечер, третий раунд): МАКЕТ 2.4 — крупный фидбек владельца по «Кто смотрит» и динамике, всё реализовано. (1) Сигаретка частоты: проценты на корзинах, воздух сверху (panel-b.tbl-wrap имел padding-top:0 — кольцо выбранной корзины резалось). (2) Список группировки раскрывается ВЛЕВО, не за край (грабля: .dd.sm .dd-body{right:0}). (3) Динамика: ось X «календарь» — метка на КАЖДОЙ точке (interval:0, требование владельца), двухстрочная rich-разметка: смена месяца (d/w) или года (m/q) вторым рядом + жирная метка смены; пилюльки ПЕРЕСЧИТЫВАЮТ стек (vis-сумма оставшихся: высота, подпись итога, потолок оси, «Всего на графике»), проценты у КАЖДОЙ ступени в тултипе; подписи чисел moveOverlap:shiftY (не прячутся); emphasis focus серий убран (мерцание в зазорах баров). (4) Активные условия — контекстной строкой чипов ПОД наблюдениями, НАД каталогом/динамикой (полоска сверху чистая, строка всегда рендерится — не прыгает; ctxReset). (5) Каталог: клик ПЕРЕКЛЮЧАЕТ выбор, Shift+клик накапливает, повторный клик по единственной снимает (pick(scope,mode,val,additive)). (6) «Что видно в данных»: единая коробка пиксель-в-пиксель для «в норме» (is-empty, старый no-insight удалён), метка «данные до вчера», «сегодня» в интерфейсе не встречается. (7) Настройки «Кто смотрит»: «Только руководители» + «Исключить логины» (S.whoHeads/whoExcl, локальные условия списка). Проверки: _test.js ЗЕЛЁНЫЙ (грабля тестов: U.chip эскейпит «&»), check.js 15 состояний. НЕ СДЕЛАНО: браузерные check.js 1920/1280 + скриншоты — браузер в песочнице не стартует, ЖДЁМ start-browser.sh от владельца.
- 2026-09-21 (поздний вечер, корректировка 2.4 по гневному фидбеку): шапка правой панели ПЕРЕДЕЛАНА на одну строку как у каталога (правая колонка вкладок+поиска в два этажа УДАЛЕНА — она растягивала шапку и «уезжал» таб «Кто смотрит»): [заголовок+sub с «…»][поиск][вкладки] — поиск вернулся наверх, крайний правый, как в каталоге. Тулбар «Кто смотрит»: [Группировка][Настройки] СЛЕВА (настройки больше не вправо), [счётчик][Свернуть все][Развернуть все] справа. Дерево по «Департаменту» стало ДВУХУРОВНЕВЫМ (повторная претензия «блок розницы должен быть с левого края»): lvl3-БЛОК у самого левого края (padding-left:6px, жирный) → lvl4-департамент отступом → люди ещё правее (grp-child.deep); клик по имени и блока, и департамента — людская шина; ключи свернутости префиксованы уровнем (B:/D:/G:), WHO_TREE_KEYS собирает их для «свернуть/развернуть все» (кнопки data-wofold). Тесты: _test.js зелёный, блок дерева расширен (блок/деп/отступ/фолд-кнопки). Урок: двухэтажная шапка панели = «чарт увеличивается, кнопки уезжают» — шапки соседних панелей сплита обязаны быть одной строки и одной высоты.
- 2026-09-21 (ночь, браузерная верификация 2.4): владелец запустил start-browser.sh → check.js 15/15 состояний ЧИСТО на 1920 И 1280; сняты и проверены глазами 6 кадров в _shots/24 (дерево блок→деп→люди, свернуть все до заголовков блоков, динамика с пересчётом пилюльки и осью-календарём «31 сен», поповер настроек в пределах панели, старт 1280 одной строкой, недели с меткой месяца). Ось на неделях: метка месяца появляется на бакете смены месяца. _shots/23 и мёртвые webp-эталоны от 09-16 удалены. Макет 2.4 ГОТОВ К РЕВЬЮ владельца. Дальше: ревью → пересборка полоски/тела виджетов под 2.4 и куб v6 (замерка .pa6_p*.sql всё ещё у владельца).
- 2026-09-21 (ночь-2, ГЛАВНАЯ ГРАБЛЯ РАУНДА, владелец был прав дважды): строки групп «Кто смотрит» стояли СПРАВА — базовое правило `.ptable td{text-align:right}` (числовые колонки) прижимало и td-строку группы (colspan) к правому краю; мои «проверки глазами» по уменьшенным скринам это дважды пропустили. Вскрылось ТОЛЬКО CDP-замером координат (ghName.left=1020 при панели 539–1256; после фикса 579). Фикс: `.ptable tr.grp-h td{text-align:left}`. Заодно из значений мока убрано слово «Блок» («Блок «Розница»»→«Розница», 20 замен в data.js: CUTS.lvl3.vals + ORG_TREE + home полей) — владелец: «не дописывать ничего к названию». ПРАВИЛО НА ВСЕГДА: выравнивание/отступы проверять ЧИСЛАМИ (getBoundingClientRect через CDP), скрин на 1371px скрывает правое выравнивание внутри широкой колонки; и «проверил глазами» ≠ «проверил» — владелец дважды указывал на то, что я «видел» и не замечал.
- 2026-09-21 (ночь-3, ИСТОЧНИКИ ПЕРВОГО ЛИСТА ГОТОВЫ): (1) Куб v6 `.gen_cube6.py` → .pa6_body.sql/.pa6_coh.sql + эталоны: на предагрегатах pa_evd_day×pa_dash_meta×pa_emp_attrs, каталог rep/grp(owner,coll)+total (ов-разрезы УБРАНЫ), джиня без полочных rep/coll/own/auth/cert_f, людская шина who = ppla (свой скан факта, JOIN dash_ok обязателен — корзины по той же вселенной, HAVING-корзины 1/2-3/4-7/8-15/16+), login_f+freq_f НОВЫЕ, state_j = JSON активных условий у total-строки (37-я колонка; ФИО-хвост осознанно). Когорты людскую шину НЕ читают (архитектура 2.5), CUTS = прямые колонки attrs. Стенд `.jt6.py` 31/31 (грабли: cut-префикс ключей CUTS потерялся; фигурные скобки JSON в SJ; %i в %-формате на одиночных {% — экранировать {%%). (2) Датасет pa_who `.gen_who.py` → .pa_who.sql: секции total/freq/ts/ctx(lvl3/lvl4/spec/stream/head/dep(k=lvl4,parent=lvl3)/adg топ-100)/list (топ-2000 по days,views), 19 колонок, джиня = period+свитки+sel области (rep-мульти/own/coll), людскую шину НЕ читает. Стенд `.jt_who.py` 14/14. Флаги FIO_EXPR/EXP_EXPR = заглушки (до DESCRIBE attrs у владельца). (3) ПОЛОСКА `Виджеты/pa-strip.*` СДАНА: период+поповер 3 свитков+сброс, maskOf/resetState чистые, эмит period_param/pub_f/act_f/exc_f (только отличия от дефолта), vm_strip 23/23, check.py: 0 FAIL, 1 ОСПОР M4 (контрол-чарт без данных — заявка на правку скилла), WARN Z4 (чипы у контента = тело/state_j). Датасет-носитель pa_dicts лимит 1 (владелец «да»). ГРАБЛИ СЕССИИ: (а) mv болванок bootstrap из подпапки затёр общий FIELDS.md проекта — восстановлен из поставки 09-19 «3. FIELDS.md»; урок: bootstrap кладёт в <папку>/<name>/, при переносе не тащить его FIELDS.md. (б) vm-тесты «opts-pop» ловили селектор в CSS — проверять class="…" в разметке. (в) check.py --accept действует на ОДИН прогон — финальный чек повторять с тем же accept. Дальше: pa-who (правый чарт) → тело → когорты+поставка.
- 2026-09-21 (вечер-2, МАКЕТ 2.5): раунд владельца по ПЕРВОЙ вкладке — «убрать галочку сертифицированных; каталог ⇄ „кто смотрит“ влияют друг на друга в обе стороны; кликать на человека и департамент; сам-на-себя сомнителен; Shift-накликивание со счётчиком — посмотри best practices». Best practices (Context7 исчерпал квоту → веб-поиск): Superset кросс-фильтры НЕ действуют на чарт-источник; Power BI cross-highlight (гасить, не убирать) предпочитают cross-filter. Сделано: (1) свиток «только сертифицированные» удалён из опций/чипов/reportRows/selectionOf (бейдж ✓ в каталоге остался); (2) клик по группе — СТРОКА целиком (подчёркнутая кнопка-имя убрана), семантика как pick(): переключает/Shift копит/повторный снимает (pickEmp), между разрезами И, внутри ИЛИ; (3) клик по ЧЕЛОВЕКУ — людская шина по логину (S.filters.emp.login, чип с ФИО через FIO_BY, счётчик «выбрано: K» в тулбаре); (4) панель себя НЕ сужает: контекст дерева от whoAreaPeopleNoBus (pickedIds(scope,true) → reportRowsBase), выбранная строка sel, пустые группы grp-dim гаснут но остаются, контент-группы вверх; содержимое списка осталось шина-калибровано (сходимость с KPI). (5) Мок: логины УНИКАЛИЗИРОВАНЫ (было 399 уникальных на 4000 — тёзки делили логин, клик подсвечивал толпу; теперь п.иванова, п.иванова2…). Хвосты: «Кто из ЦА» поимённо без кликов; кубу v6 нужна логин-колонка в людской маске. Проверки: _test.js зелёный (+блок человек/дерево/Shift), check.js 16 состояний ×1920/1280 чисто (состояние certified заменено на published), CDP-зонды числами (старт 60/1768 → человек 1 → блок 60/449, корзины=449, блок∩человек=1), скрины _shots/25 (4 кадра, 24 удалён), док → «Архитектура 2.5.md» (2.4 удалён), README/howModal подправлены. ГРАБЛИ РАУНДА: NodeList в prep-скрипте до клика = СИРОТЫ после ререндера, MouseEvent не доходит до document-делегата (shift-клик молча терялся — переснимать селекторы после каждого клика); числа на сжатом скрине прочёл дважды неверно — только CDP-дамп. Дальше: ревью владельца 2.5 → пересборка полоски/тела виджетов и куб v6 (замерка у владельца; добавить требование логин-колонки).
- 2026-09-21 (вечер-3, ЧАРТ PA-WHO СДАН): «Кто смотрит»⇄«Динамика» собран по скиллу (РЕЖИМ A): Виджеты/pa-who.chart.js 1523 строки, блоки 1–7 (5 разбит на a-C), CFG ns=pwho 20 полей == data.sql == FIELDS.md виджет 6; динамика — ПОРТ из тела (SVG стек/линия, легенда-пилюльки, следящий тултип onMove+data-bk), гранула выводится из числа ts-бакетов (30/20/12/8). Людская шина: клик группы/человека/корзины → applyCrossFilter(lvl3_f/lvl4_f/spec_f/stream_f/adg_f/heads_f/login_f/freq_f), сброс [] по паттерну ПОЛКИ (прецедент 783469; пустой [] у тела падал в др. контексте 783708 — комментарий в maskOf); локальное сужение списка: настройки→picks→корзина→поиск, adg ЛОКАЛЬНО не сужает (членства в ответе нет — хвост §6). Дерево: контекст-счётчики с сервера (ctx/dep/adg), «текущая выборка» локально, контент-группы вверх, grp-dim. Сдача: check.py 0 FAIL (WARN S18 — динамическое присваивание state[data-view-префикс] валидатор не видит; WARN Z4 — 4 хвоста), vm_who.js 35/35 (эмиты, дерево, свёртки, настройки, поиск RETRO-68, noData), браузер SEGV в песочнице — экрана не видел. ГРАБЛИ: (а) запятая в конце элемента массива CSS рвала конкатенацию — «+строка» становилась унарным плюсом → NaN в собранном CSS, T3 это поймал; (б) vm-тесты считали селектор в CSS-тексте (grp-child в <style>) — считать только class="…; (в) общий SELF_CHECK.md на папку: check.py перенёс машинную часть на pa-who, но СУЖДЕНИЯ остались от pa-strip — перезаполнять руками при каждой сдаче в этой папке; (г) NOTES §2: частичные диапазоны app.py валидатор считает незакрытым покрытием — источники оформлять ССЫЛКАМИ без номеров строк (конвенция pa-audience). data.sql болванки НЕ остаются: у тела осталась незаполненной — сверять при закрытии SQL-этапа (повтор урока pa_aud). Мок пополнен корзиной 1 и «вчера». Дальше: пересборка pa-reports-body под 2.5 → когорты → поставка первого листа.
- 2026-09-21 (ночь-4, ТЕЛО V6/2.5 ПЕРЕСОБРАНО И СДАНО): pa-reports-body.chart.js 2360→1791 строк. Что вошло: каталог = ЕДИНСТВЕННАЯ панель full-cell (cls:prb-cat, flex-колонка; ячейка борда узкая ~30–40% — KPI-сетка auto-fit minmax(150px,1fr), шапка «Отчёты»+«область:»+свежесть); 3 разреза (report/collection/owner, людские в pa-who); НОВОЕ stateChips() — информационные чипы state_j из total-строки (37-я колонка куба v6): отклонения свитков («включая неопубликованные»…), люди через ppl()-коллектор (1 → именованный чип, 2+ → «Люди: N» с тултип-строками), «Частота: …» — БЕЗ крестика (снятие в своих чартах, тултип объясняет). Вырезано ~570 строк мёртвого: вся динамика (SVG-сборщики, tabsHtml, dynTipHtml, placeTip/onMove, mousemove), syncSvgWidth+resize-слушатель (SVG_W исчез — ReferenceError-мина убрана), частота (freqRows/freqSlice/freqstrip/data-freq-ветка), ov-ветки домена, -split/-dynhead/-legend/-fc-* CSS; селекторы каталога -split-l→-cat (3 места); onClick unchip упрощён (freqSel нет). MAU-карточка осталась (ts_users). Сдача: validate 0 FAIL, check.py: WARN T7 (сканер не видит конкатенированные data-tip=/data-action= — собираются строками tip()/кнопками пейджера) + WARN Z4 (15 хвостов: 13 унаследованных + 2 новых: логины вместо ФИО в «Люди: N»; «область:» не знает людскую шину), vm5.js ПЕРЕПИСАН под v6 (мок 37 колонок: rep25+grp4+total со state_j; 51/51: разрезы, кросс-фильтрация вкладок, чипы обоих видов, «Люди: 3», кэш пустого ответа, noData; когортная часть не тронута). Эталон: Виджеты/pa-reports-body.html = дамп .render/_dump_body.js (зоны kpi+obs+ctx+catalog макета 2.5 — strip/right/pret отдельные чарты; прежний файл был болванкой bootstrap). data.sql тела собран из .pa6_body.sql + --FIELDS-- 37 (хвост «болванка» закрыт); FIELDS.md виджет 1 → v6 + чек-лист (замена SQL 169602, +state_j в Измерения, JS чарта). ГРАБЛИ: (а) zsh-песочница ест \r в python -c однострочниках с кавычками — многострочные правки делать Write-файлом; (б) при вырезке блока следом grep-ить ВСЕ имена вырезанного (mousemove остался с другой шириной отступа, tabsHtml жил вторым вызовом в panelHtml-тернаре o.tabs); (в) vm-ожидание «колл. B → 1 владелец» было неверным к моку (чётные отчёты дают обоих) — тесты писать от данных мока, не от памяти о v5. Дальше: когорты (джиня v6 уже в .pa6_coh.sql, чарт не менялся) → папка поставки первого листа 2.5 после ревью владельцем JS+SQL.
- 2026-09-21 (ночь-5, ПОСТАВКА ПЕРВОГО ЛИСТА 2.5 СОБРАНА): папка «Обновление 2026-09-21 (v6: первый лист 2.5)» — 0. Инструкция + 1/1a SQL тела v6 (169602), 2/2a SQL когорт v6 (169589, форма не менялась — чарт и Измерения не трогаем), 3 JS тела 788805, 4/4a датасет pa_who (20 Измерений, лимит 5000), 5 JS чарта «Кто смотрит» (самовлияние ВЫКЛ, эмит → 788805), 6 JS полоски (носитель pa_dicts, лимит 1, эмит → тело+когорты+who), 7 FIELDS.md. Ключевое в инструкции: ско Scope 788805 меняется — ДОБАВИТЬ who в получатели эмита каталога (было «только pa_cohorts»); после замены SQL тела — «Синхронизировать столбцы» + добавить state_j (37-я); полку pa-shelf с листа СНЯТЬ; раскладка: полоска сверху / тело ~30–40% слева / who справа / когорты полноширинно; стоп-точки: тело >12с в SQL Lab → откат (папка 2026-09-19 остаётся до применения v6, потом удалить). Замерка .pa6_p1…p5 удалена (закрыта дефолт-рендером 1a). CLAUDE.md обновлён: белый список (+.gen_cube6/.jt6/.pa6_*/.gen_who/.jt_who/.pa_who_*), навигация на v6-инструменты, архитектура «три шины 2.5», карта Виджеты (+pa-who/pa-strip, полка помечена снятой). Когорты: чарт/виджет НЕ менялись (форма 8 колонок та же) — отдельной сборки не потребовалось. Дальше: применение владельцем по инструкции → фидбек → второй лист (Аудитория 2.5) и describable-хвосты (ФИО/стаж/adg-членства после DESCRIBE pa_emp_attrs).
- 2026-09-22 (Code 53 от владельца, починено): при создании датасета pa_who ClickHouse ронял запрос «UNION different number of columns» — в шаблоне у 7 из 10 плеч (total + все ctx, и freq) отсутствовало NULL AS react_u: 19 колонок против 20 у ts/list. ДВЕ причины: (а) при добавлении react_u (ревизия +react_u) списки-заглушки n2/n4/n5 в .gen_who.py не получили react_u; (б) .pa_who.sql на диске был СТАРЕЕ генератора (n0 уже имел react_u — перегон вернул колонку в total-плечо). Почему не поймали: jt-стенды сверяют рендеры шаблона и генератора МЕЖДУ СОБОЙ (обе стороны одной битой), а SQL до боя ни разу не исполнялся; дефолт-рендер 4a владелец не прогонял до вставки. Фикс: n2/n4/n5 + react_u, регенерация (jt_who 14/14), поставка 4/4a и pa-who.data.sql перезаписаны. СТРАХОВКА НАВСЕГДА: check_arms() в .gen_who.py — механический счёт колонок каждого плеча UNION (раскрывает SELECT * FROM (…), depth-0 запятые) на каждом перегоне, падает при расхождении; ad-hoc прогоном сверены и остальные SQL: pa_aud 7 плеч × 12 ок, кубы v5/v6 и pa_dicts БЕЗ UNION (agg+JOIN) — грабля была только у who. ПРАВИЛО в CLAUDE.md (архитектура, п.4). Владельцу: переприменить файл 4, 4a теперь зелёный.
- 2026-09-22 (скрины владельца, ТРИ бага pa-who, все починены): (1) ГЛАВНЫЙ, данные: b.days считался countIf по гриду (отчёт×день×логин) — это ПАРЫ, не дни ([Login28] «1614 дней» = пары за 30 дней) → все корзины частоты нулевые кроме «16+» (100%), все «Постоянный», сортировка по парам; фикс: uniqExactIf(dte, k<n) в .gen_who.py. (2) Счётчик печатал весь серверный срез («2 000 человек») вместо макетного «60 человек из 1 768»: теперь nShown=min(список, listShow) + « из M» при M>N. (3) Ось динамики шла НОВЫМИ ВПЕРЕЁД: при порте из тела потерян reverse() после сортировки ts по k (тело: старые слева/новые справа; who: k по возрастанию без реверса). Прогоны: jt_who 14/14, vm_who 36/36 (мок: даты «вчера» бампаются при смене суток — тест зависим от даты!), check.py 0 FAIL. Поставка 4/4a/5 и pa-who.data.sql перезаписаны. УРОК: скрины владельца НОСИТЕЛЬ ИНФОРМЫ — «1614 дней» физически невозможно при окне 30 → countIf по гриду считал пары; сравнивать числа с ФИЗИЧЕСКИМИ пределами окна. «Стаж —», ФИО=логин, «область: выбор каталога» — известные хвосты attrs, НЕ баги этой правки.
- 2026-09-22 (владелец меняет архитектуру первого листа): «не нравится, как работают клики — долго, ужасное поведение датасета; давай общий тестовый датасет для SQL lab». Его идея: ВЕСЬ верх одним датасетом/чартом + когорты вторым; список людей ≤500 (или 1000) с подписью «первые 500»; беспокоился про «борды человека». Мой ответ: (а) честно признал — «чарт не может себя перечитывать» это НАШЕ решение v5, не ограничение платформы (прецедент 59922, в v3 клики были серверными ~1.2с); (б) два варианта: v7-один-чарт с самовлиянием (клик по отчёту → полный список смотрящих, клик по человеку → его борды из dashes) vs без самовлияния (топ-500 + локальное сужение по dashes-массиву); (в) собран ЗАМЕР .gen_cube7.py → .pa7_{default,sel_report,sel_login,month}.sql: форма 49 = 37 куба + 12 людей (login,fio,lvl3,lvl4,spec,stream,exp,is_head,days,last_dt,bin,dashes — топ-20 бордов человека строками); люди поверх того же b0 (CSE, один скан факта); days=uniqExactIf (урок pa_who); dep-родитель в cut_val; adg ARRAY JOIN LIMIT 100; list LIMIT 500; сел-рендеры: борд 13896, логин so.zhdanova (из скрина). Сверка 9 плеч × 49 в генераторе. ГРАБЛИ PYTHON-ПРАВОК: replace с '\n' в однострочнике = литеральный бэкслэш-n, замена молча не применяется (дважды!) — многострочные тексты править Edit-инструментом, не python -c с 
. ЖДЁМ цифры от владельца: вход/клик/12мес; порог разговора: клик ≤1.5–2с → самовлияние живо.
- 2026-09-22 (замер v7 по кругу с владельцем, три порт-ошибки — все мои, все починены в генераторе): (1) Code 386 NO_COMMON_TYPE — в прототипе ключи каталога кортежем [(0,''),…,(3,cn)], а cn=Array(String) — массив в кортеже со скалярами не имеет супертипа; фикс: перенос боевого приёма v6 arrayMap(c -> (3,c), cn) по кортежу на коллекцию; (2) Code 215 NOT_AN_AGGREGATE — при копировании CTE-цепочки потерян login в GROUP BY агрегата pa (v6: GROUP BY kd,k0,login — метрики на человека); (3) Code 70 CANNOT_CONVERT_TYPE «NULL → non-nullable» — LEFT JOIN pa_dash_meta ON dashboard_id = toInt32OrNull(k0): Nullable-ключ в JOIN ON; v5.1 обходил через CTE meta с GROUP BY (обе стороны из одного источника), v6/v7 джойнили таблицу напрямую; фикс: ifNull(toInt32OrNull(k0), toInt32(0)) — сентинел 0 (id не существует, семантика LEFT JOIN не меняется); ВАЖНО: v6 поставки нёс ту же мину (никогда не исполнялся) — файлы 1/1a и data.sql тела перезаписаны, jt6 31/31. УРОК: CTE-цепочку ПОРТИРОВАТЬ ТОЛЬКО БАЙТ-В-БАЙТ (мех. дифом с прототипом), не пересобирать по памяти; и «стенд зелёный» ≠ «SQL исполняется» — jt-стенды сравнивают рендеры, исполнимость видит только ClickHouse.
- 2026-09-22 (вечер, новый агент, ветка `new`, ПОСТАВКА v7): аудит v6 по запросу владельца «максимум логически связанного — в один чарт, расчёты на фронте, отдельно — только другая гранулярность; финальные SQL с джиней, предагрегаты в GP». Решение владельца: лист «Отчёты» = 3 чарта (полоска на pa_strip `SELECT 1` · каталог 788805 без KPI · «Аудитория области» pa-area на новом pa_people: KPI + Кто смотрит · Динамика · Закрепляемость). Новый GP-предагрегат pa_pair (пара отчёт×логин, 64-бит маски бакетов для d/w/m/q, own_flg, md), pa_emp_attrs += fio, exp_nm. Куб v7 на pa_pair (16 колонок), запасной на дневном факте — те же числа. pa_people — одно чтение факта, роли строки одним arrayJoin (pa_who читал факт 11 раз, pa_aud 7). Проверка — новый стенд `.stand/` (chdb = встроенный ClickHouse, синтетика той же схемы), GP-SQL прогнан в PostgreSQL 16 (pa_pair байт в байт). Найдено и закрыто 9 багов v6 (см. CONTEXT §0). Поставка: `Обновление 2026-09-22 (v7: три чарта, предагрегат пар)`; ждёт применения, v6/полку — удалить после приёмки.
- 2026-09-23 (ветка `new`, ВАРИАНТ А по фидбеку владельца со скрина живого борда: «верхний ряд — как два дашборда рядом»): шапка листа во всю ширину (~290 px) = чарт pa-strip на НОВОМ датасете pa_kpi (одна строка: KPI всего экрана = область каталога ∩ людская шина, только pa_pair, факт не читает; area_nm/ppl_nm — подписи пилюль, state_j — все условия) — полоска + 5 KPI + «Что видно» (карусель ‹ n/N ›, фикс. высота: чарт не может сдвигать соседей) + пилюли ВСЕХ фильтров (область/люди — эхо, снимаются в своём чарте; опции — с ×). Под шапкой две карточки: каталог 38% (шапка/чипы/KPI убраны, «снять выбор» в подзаголовке) | панель 62% (вкладки Динамика — по умолчанию и растянута на высоту чарта через syncDynH · Кто смотрит · Закрепляемость; KPI убраны). Типографика мягче: 600/700 → 500/600, текст таблиц ink2. Скоупы: шапка → всё + себя (самовлияние ВКЛ), каталог/панель → друг друга + шапка. Регресс check_v7 82/82 (+ pa_kpi == total pa_people по всем 11 KPI, == ИТОГО тела под шиной, == строка отчёта). Стенд листа — `.stand/board.mjs` (3 iframe, клики, скрин). Поставка v7 обновлена: файл 4 = pa_kpi (+4a), 7 = JS шапки.
- 2026-09-23 (фидбек владельца с боя по варианту А): (1) вкладки каталога не фильтровали друг друга по коллекциям — ПРИЧИНА: строковый Array ClickHouse Superset отдаёт через str() → Python-вид `['a', 'b']`, JSON.parse падал, colls=[] (на стенде воспроизведено: старый JS + такой мок = пустая вкладка «Коллекции»). arr() теперь разбирает JSON / Python-вид / живой массив (в pa-reports-body и pa-area); пики групп — строки (логин «12345» числом не совпадал). ПРАВИЛО: строковые массивы из датасета — только через arr(). (2) Настройки «Кто смотрит» (только руководители, исключить логины) стали шиной: heads_f='1', новый носитель exl_f (NOT IN) в каталоге/2b/pa_kpi, пилюли «Только/Без руководителей», «Исключено: N». Баг: группа «Остальные» слала heads_f=1 — теперь 'n' (management_head_flg = 0). (3) Поиск каталога/панели — 34 px. Регресс 95/95. ГРАБЛЯ: check_v7 читает SQL из ПАПКИ ПОСТАВКИ — после правки Виджеты/*.data.sql сначала копировать в поставку, потом гонять.
- 2026-09-23 (решение владельца: «KPI внутри правой панели», раскладка в ДВА УРОВНЯ): уровень 1 — тонкая шапка pa-strip (~100 px) на ЛЁГКОМ датасете pa_strip (4 колонки: grain, area_nm, ppl_nm, state_j; без выбора таблиц не читает) — период · опции · свежесть · сброс + ряд пилюль; уровень 2 — каталог 38% | панель 62% (≈1000 px), в панели сверху 5 KPI + «Что видно» (карусель, порт из шапки варианта А) из total pa_people, ниже вкладки. pa_kpi снят (шапке KPI больше не нужны). Самовлияние у всех трёх выключено. Регресс 82/82 (+ группы коллекций/владельцев каталога == KPI панели, эхо шапки). Людская шина сужает только каталог; KPI панели — про всю область.
- 2026-09-23 («Кто смотрит» по фидбеку владельца: «при группировке — компактная таблица с суммариками, сортировка; УС-3…УС-7 одной опцией; люди — пагинация, выгрузка, копирование»): GP pa_emp_attrs += lvl5…lvl7 (есть в proteus_views_1); pa_people: ctx g='org' — узлы оргструктуры, k = путь «УС-3 › … › УС-7» (до первого пустого уровня), parent = путь родителя, полные метрики группы; у list parent = путь человека; LIST_N 3000, лимит строк чарта 10 000; HAS_ORG=false — дерево до УС-4. Новый носитель org_f (пути любой глубины; SQL сравнивает arrayStringConcat первых L колонок, УС-5…7 трогаются только при узле такой глубины) в каталоге/2b/шапке (пилюля «УС-N: звено»). Панель: Вид «Люди» (сортировка по колонкам, пагинация 50, Копировать = TSV в буфер с fallback execCommand, CSV «;»+BOM — в песочнице Proteus скачивание может быть запрещено) | группировки (org/spec/stream/adgroup/heads) — сводная таблица: строка «Итого по области», колонки Людей·Доля(полоса от крупнейшей группы)·Δ к пред.(только d/w)·Просмотров·На чел.·Постоянных·Новых, сортировка по заголовку (у каждого уровня — свои соседи), ▸ раскрывает вглубь, у узла — вложенная таблица людей (15, «ещё» до 300), «Раскрыть уровень»/«Свернуть все», колонка «В выборке» при локальных условиях. Экспорт — из модели (все уровни групп / все люди списка). Регресс 92/92: узел любой глубины → ИТОГО каталога == людям узла. ГРАБЛИ: регэксп с «"» внутри ломает разбор validate.py (S15) — кавычку через String.fromCharCode(34); querySelectorAll('td[...]') валидатор принимает за DOM-экспорт (V1).
- 2026-09-23: каталог — иконка «ссылка» у каждого отчёта (data-replink): копирует <origin борда из document.referrer, иначе https://CFG.dashHost> + CFG.dashPath ('/superset/dashboard/') + id + '/' (путь из dashboard_url макета, в бою не сверен; литерал URL в коде валидатор режет как CDN — D1c), строку не выбирает, ✓ на 1,8 с. Копирование: Clipboard API → fallback textarea+execCommand (в песочнице/не-secure context navigator.clipboard нет — проверено на стенде, fallback копирует верный URL).
- 2026-09-23 (бой, сохранение датасета pa_strip): «unsupported operand type(s) for +: 'AlwaysTrueObject' and 'list'». ПРАВИЛО: при сохранении/синхронизации датасета Proteus отдаёт из filter_values НЕ список, а AlwaysTrueObject (истинный, итерация пустая). Никаких операций со списками фильтров, кроме итерации: каждый списочный фильтр строится циклом `{% set x = [] %}{% for v in (filter_values('..') or []) %}…append…{% endfor %}`; конкатенация `a + b` запрещена. Стенд: stand.render(..., always_true=True), в check_v7 — рендер+исполнение всех SQL поставки в этом режиме.
- 2026-09-23 (фидбек: «таблицы по-разному отформатированы, в списке людей/группировке нет скролла»): общий блок CSS таблиц «── ТАБЛИЦЫ: общий стиль … ── /ТАБЛИЦЫ ──» ДОСЛОВНО одинаков в pa-reports-body и pa-area (шапка 10,5px uppercase, строка 44px, первая колонка 500/ink + unit-sub вторая строка, прочие текстовые 400/ink2, выделение — синий фон + полоса слева на td, стрелка сортировки только у активной колонки, итог tr.tot, пагинатор pginfo/pgnum/pgbtn «Показано a–b из N», cellbar). ПРАВИЛО: правка таблицы — в обоих файлах сразу, блоки сравнивать (python: regex по маркерам). Группы «Кто смотрит» — второй строкой «УС-N · K подразделений» вместо бейджа. Скролл: -list-zone flex-колонка (flex:1;min-height:0), -panel-b.tbl-wrap overflow:hidden, скроллится -tbl-scroll.
- 2026-09-23 (UX-ревью по просьбе владельца «кнопки не интуитивно, паддинги, всё в куче»): найдено и исправлено — (1) БАГ: поповер «Опции» в шапке ~100 px обрезался iframe-ом, опции нельзя было включить → переключатели «Считать: Опубликованные · Актуальные · Без владельцев» прямо в строке (ПРАВИЛО: в тонком чарте никаких поповеров вниз — iframe режет); (2) подсказка кнопки висела поверх раскрытого меню (Chrome шлёт наведение после перерисовки) → onOver пропускает aria-expanded="true" + hideTip при открытии; (3) KPI переносились 4+1 при 1280 и «к пред. 30 дням» ломал плашку в 2 строки → сетка ровно 5 колонок, сравнение во всплывашке, подпись «Пользователей» без периода; (4) поиск «Кто смотрит» из заголовка — в тулбар (заголовок одинаков на всех вкладках), тулбар: слева вид·настройки·поиск, справа счётчик·раскрытие·Копировать·CSV; ввод пересобирает только таблицу и счётчик (фокус держится); тост — плавающий; (5) каталог: поиск в одной строке с заголовком (гибкая ширина), подпись «В разрезе» убрана.
- 2026-09-23 (фидбек: «фильтры — в самих чартах, клик не должен менять вёрстку; сигаретка режется; корзины частоты не по гранулярности»): строка «Выбранные фильтры» над каждым чартом (каталог, панель) на сером холсте, ФИКСИРОВАННАЯ высота 34 px: своё — пилюли с × (data-unpick, «Снять все»), чужое (эхо соседнего чарта: каталог — state_j куба, панель — area-строка) — белые справочные. Общий CSS-блок «ВЫБРАННЫЕ ФИЛЬТРЫ» и filterRowHtml ДОСЛОВНО одинаковы в двух виджетах. Шапка — одна строка (период · «Считать» · свежесть), без пилюль и «Сбросить», ~64 px. Сигаретка: padding-top 5px (кольцо .on выступает на 3 px). Корзины частоты — FBIN по гранулярности (d/w [1,3,7,15], m [1,3,6,9], q [1,2,3,4]; q — потому что в 13 мес. истории больше 5 кварталов не бывает) в pa_people, каталоге, 2b и JS (CFG.fbins/fopen); выбор хранится номером корзины и при смене периода переходит в ту же по счёту. Регресс: корзины делят всех людей периода и сходятся с ИТОГО каталога во всех 4 гранулярностях (115/115).
- 2026-09-23 (бой, pa_people после обновления GP): «Code 386 There is no supertype for types Int64, UInt64» — if(length(lv) [UInt64], arrayFirstIndex(...) - 1 [Int64]). chdb (новее) молча давал Variant. ПРАВИЛО: ветки if/multiIf/массивы одного типа явно (toUInt32/toInt64 в КАЖДОЙ ветке); стенд теперь стартует с SET use_variant_as_common_type = 0 — ведёт себя как CH 24.
- 2026-09-23 (фидбек: «клик — правая часть думает 6–7 с, каталог быстро»; «над правым графиком не дублировать фильтры соседа»): pa_people переведён с дневного факта на pa_pair + новый GP-предагрегат pa_dash_bkt (dashboard_id, grain, k, views, views_nown; k < n; сверен с GP-SQL в PostgreSQL 16 построчно). Стенд big: весь Proteus 1,43 → 0,64 с, 20 недель 2,08 → 0,75 с (каталог 0,71). «Дней» у человека = активных ПЕРИОДОВ грануляции (JS: колонка «Дней/Недель/Месяцев/Кварталов»). Запасной на факте — поставка файл 3b (дни приведены к той же семантике), check_v7 сверяет 3 == 3b построчно. Строки «Выбранные фильтры» — ТОЛЬКО своё снимаемое (ext = []), чужое не показывается. Совет владельцу: «Тайм-аут кэша» 3600 у датасетов. ПРАВИЛО: rows_read из stand.stat считает ВСЕ таблицы — «не читает факт» проверять по тексту рендера.
- 2026-09-23 (фидбек: «тултипы как в eCharts — за мышью, и в таблицах; в «Кто смотрит» тултипы не нужны; ссылка не влезает в тултип; переключалка каталога прыгает на 1 px при клике»): showTip в обоих виджетах понимает якорь-точку (rect.pt): onOver кладёт curPt(e), onTipMove на mousemove только двигает (HTML пересобирается, лишь если другой — tip.__h, сброс в hideTip и в тултипе динамики). Строки таблиц «Кто смотрит» (люди, группы, вложенные) — без data-tip; заголовки колонок с пояснениями оставлены. Кнопка ссылки — короткая подсказка без URL. Прыжок: бейдж счётчика 15 px выше строки текста раздувал кнопку вкладки (30→33 px) → -sub-tab фиксированной высоты 26 px (tiny 22), inline-flex. ПРАВИЛО: всё, что появляется по клику внутри кнопки/пилюли (бейджи, счётчики), — в элементе фиксированной высоты.
- 2026-09-23 (вопрос владельца: «не грузить “Кто смотрит”, пока не открыта вкладка — через джиню»): замер big-стенда — без ctx/list/freq pa_people весь Proteus 0,51→0,23 с (×2,2), один отчёт 0,15→0,10 (×1,5), коллекция 0,22→0,15. Джиня умеет, но вкладка — внутреннее состояние чарта: до своего запроса она доходит только через самовлияние (тогда каждый клик по людям перезапрашивает панель саму себя) либо если разнести на два чарта во вкладках борда Proteus (скрытая вкладка не грузится). Решение за владельцем.
- 2026-09-23 (бой: клик по отчёту 3–5 с; SQL Lab 3c — pa_people одного отчёта 2,33 с при стенде 0,15 с; system.query_log в SQL Lab ЗАПРЕЩЁН — «Использование system запрещено»): причина — ad_groups. У сотрудника на бою ~сотни AD-групп, в синтетике было 1–2. Воспроизведено на big-стенде (ad_groups ~110 на человека): 1 отчёт 0,16 → 1,17 с, весь Proteus 0,5 → 2,7 с; чтение массивов ≈ половина, разворот ролей ctx adg в arrayJoin — остальное. Фильтр справочника по людям области почти не помог. РЕШЕНИЕ: `{% set WITH_ADG = false %}` в pa_people (и 3b) — роль adg не строится, ad_groups не читается (1 отчёт 0,16, весь 0,46); JS прячет вид «AD-группа», если gm.adg пуст (groupsNow). ПРАВИЛО: синтетика стенда должна повторять кардинальность боевых массивов (ad_groups ~100+), иначе замеры врут; замер клика — файл 3c (рендер под один отчёт, id подставляет владелец). big-стенд (scratch pa_big) пересобран с толстыми ad_groups.
- 2026-09-23 (фидбек: подпись «Выбранные фильтры» дублируется над каталогом и панелью): подпись одна на лист — над каталогом. filterRowHtml(own, ext, hint === null) рисует строку без подписи и подсказки той же высоты; панель передаёт null. Общая функция в двух виджетах по-прежнему дословно одинакова.
- 2026-09-23 (фидбек по закрепляемости и оси): ось X — линия axisLine по ширине колонок в calAxisSvg (оба графика динамики). Когорты: отступ 14 px справа у столбика «Пришло»; строка медианы -ct-med в шапке есть ВСЕГДА (в режиме «от таблицы» — &nbsp;, у «Когорта/Пришло» тоже) — шапка не прыгала 32→20 px; шкала раскраски асимметрична: край вверх = макс. отклонение вверх, вниз = макс. вниз (spanUp/spanDn) — раньше симметрична по |макс|, один край легенды пустой (наведение ничего не подсвечивало). ПРАВИЛО: крайняя ступень легенды обязана подсвечивать хотя бы одну ячейку.
- 2026-09-23 (фидбек: «Что видно в данных» пусть расширяется вниз, а не листается вправо): карусель ‹ 1/N › заменена раскрывашкой — строка-заголовок с первым фактом и кнопкой «Ещё N ▾» (data-obs=toggle, state.obsOpen); раскрыто — список всех фактов (точка важности, заголовок, пояснение). Графики динамики ужимаются сами: syncDynH меряет остаток тела панели после render.
- 2026-09-23 (фидбек: две пилюли не помещаются): строка фильтров — 1 пилюля во всю ширину (… у длинного), 2 — .two max-width 50% каждая + «Снять все», 3+ — одна сводная «Применено N фильтров» (список во всплывашке, × = data-unpick *). Пилюли flex:0 1 auto;min-width:0 — сжимаются с «…», не вылезают.
- 2026-09-23 (фидбек: скрепка ссылки слева, перенос названия с отступом): ячейка отчёта — -rname (flex): кнопка -lnkbtn слева, текст -rname-t (название + unit-sub владельца) блоком справа — висячий отступ, вторая строка не уходит под иконку.
- 2026-09-23 (фидбек: корзины «Кто смотрит» не сходятся с KPI, клик по корзине должен показывать всех; «Постоянные» KPI неверны на фоне таблицы): (1) список — ВСЕ зрители, упакованные: одна строка list на путь оргструктуры, k = люди через \n, поля через \t (логин, ФИО, спец, стрим, стаж, рук., периодов, просмотров, визит, корзина); JS parseListPack. Весь Proteus big-стенд: 42 тыс. людей, 1,2 тыс. строк, 3,7 МБ, 0,48 с (построчно было бы 21,6 МБ). Лимит строк чарта → 50 000. ГРАБЛЯ CH: алиас NULL AS login в том же SELECT перекрывает колонку login — в упаковке ссылаться через таблицу (lp.login). (2) постоянные = корзины 4–5 на любой грануляции: REG d/w 8, m 7, q 4 (было ≥8 везде: на кварталах всегда 0, на месяцах сегмент ≠ KPI) — pa_people (nb > FBIN[2]), каталог regular_users ({{ REG }}), 2b/3b, подсказки JS (grains[].reg). (3) доли корзин <10% — с одним знаком (3,0% + 0,4% = 3,4% KPI). Регресс +8: список == users, постоянные == корзины 4–5 по всем грануляциям (138 OK).
- 2026-09-23 (фидбек: каретка сводной — мелкая цель, промах фильтрует строку; мало воздуха под корзинами; оставить только «Копировать» иконкой + кнопка «список на весь чарт»; все кнопки одной высоты по поиску каталога): каретка -gh-caret 28×28 с подложкой; segstrip снизу 18, who-bar снизу 12; CSV убран, копировать и «на весь чарт» — -ibtn 34×34 (state.whoFull прячет -top: KPI+«Что видно»; корзины и настройки остаются); ЭТАЛОН ВЫСОТЫ 34 px: поиск, dd-trg, -btn, -ibtn, подложки вкладок (вкладка 28), сегмент периода (кнопка 30), переключатели шапки. Сброс state.tip при переключении, иначе renderTip показывает старую подсказку.
- 2026-09-23 (фидбек: убрать «Раскрыть уровень»/«Свернуть», общая каретка на «Итого по области»): каретка в g-tot (ДС 4.6c), та же -gh-caret 28×28 и та же вертикаль, что у строк; ничего не раскрыто — ▸ (data-wofold=level, на уровень вглубь), что-то раскрыто — ▾ (data-wofold=all, свернуть всё). Отступление от ДС 4.6c (▾ только когда раскрыто ВСЁ): дерево оргструктуры целиком — тысячи строк.
- 2026-09-23 (фидбек: в выпадашке группировки — конкретный уровень УС или всё дерево; уровни — подпунктами дерева с отступом): CFG.groups + org3…org7 (sub: true, trg «Оргструктура · УС-N»), -dd-opt.sub padding-left 26; orgLevelOf(cut); rootNodes: плоско все узлы gm.org с длиной пути L-2 (id = cut:k, flat — без детей), вторая строка «УС-L · путь до него»; peopleIndex: люди поддерева (префикс пути); шина та же org_f. groupsNow скрывает уровни без узлов в данных. Кто не дошёл до УС-L — в таблицу уровня не входит (пояснение в сноске).
- 2026-09-23 (фидбек: не выкидывать тех, кто не доходит до уровня, — строка с прочерком): в плоском виде УС-L последняя строка «—» (ключ ORG_NONE, noBus: без data-who, в шину не идёт), метрики = итог области − сумма узлов уровня (узлы уровня не пересекаются → users точно; users_prev приблизительно: узлы с users=0 при разборе отброшены); ▸ — люди с путём короче L-2. Сумма строк = «Итого по области» на всех уровнях.
- 2026-09-23 (фидбек: «…»-строки в уровне УС; корзина частоты не влияет на сводную и KPI; постоянные от 6 дней, 4 корзины): (1) заглушки оргструктуры (строка из пробелов/знаков препинания: «-», «—», «…») в pa_people = пусто (Jinja-макрос ou(col), match '^[\\s\\p{P}]*$'), путь обрывается — такие люди в одной строке «—»; фильтр каталога org_f не менялся (сравнивает первые L колонок, а в пути только настоящие значения). Стенд: lvl5='-' у number%13=0, регресс проверяет отсутствие заглушек в путях. (2) корзина частоты / «только руководители» / исключения → KPI (effKpi) и итоги групп (localGM → LOCAL_GM в makeNode, пустые узлы скрыты) считаются в чарте по упакованному списку; в пакет добавлены new_u, mau, mau_prev (поля 10–12); дельты «не сравнивается», в подписи «из N в области»; колонка «В выборке» — только поиск и выбор групп. (3) корзины 4: FBIN d/w [1,5,15], m [1,3,6], q [1,2,3]; постоянные = корзины 3–4 (REG 6/6/4/3); freq_f принимает только 1–4; freqSel вне 1–4 сбрасывается. Регресс 140 OK.
- 2026-09-23 (фидбек: «Пост.» не сортируется по убыванию; в тултипе строки — постоянные абс.+%, вместо общих просмотров — на пользователя; ID отчёта у скрепки; счётчик на вкладке «Кто смотрит»): сортировка regular_users — по доле rs (колонка показывает долю; по абсолюту порядок выглядел случайным), тай-брейк по users; тултип строки и групп каталога: Пользователи · Постоянные «N · X%» · Просмотров на пользователя; linkTip(id) — ID отчёта; вкладка «Кто смотрит» — сиреневый -sub-cnt.ppl = whoFilterCount() (выбор + корзина + «только руководители» + исключения).
- 2026-09-23 (фидбек: пагинаторы каталога и «Кто смотрит» не на одной линии; подсказка «клик по …» под таблицей дублирует подзаголовок): whoTableHtml — без -tbl-note (и в «Людях», и в сводной); низ таблицы — только пагинатор, прижат к низу карточки, как в каталоге (стили -pager общие). Проверено на борде: «Показано …» и ‹ › на одной линии при одинаковой высоте чартов.
- 2026-09-23 (бой: в сводной «Кто смотрит» строки «…» без «УС-N»): НЕ данные — вёрстка. После увеличения каретки до 28 px (−6/+4 поля = 26 px) у -gtx осталось max-width calc(100% − 24px): длинное имя дорастало до максимума, содержимое ячейки на 2 px шире td, и text-overflow:ellipsis ячейки заменял inline-block ЦЕЛИКОМ на «…». Фикс: max-width calc(100% − 34px) + у .gh-name своё многоточие. ПРАВИЛО: inline-block внутри nowrap-ячейки с ellipsis обязан помещаться с запасом — иначе «…» вместо всего блока; при смене размера соседнего элемента пересчитать max-width. Проверка: td.scrollWidth <= td.clientWidth на длинном имени.
- 2026-09-23 (фидбек: кривая удержания — не влезает первый период, растянуть по высоте): retCurveSvg — поля 28/28 под подписи крайних точек; высота COH_H = остаток тела вкладки (syncCohH, как syncDynH; svg помечен data-coh-curve); шкала от 0 до max×1,25 (шаг 10, ≤100), сетка — четверти yMax (до 100% кривая 20–30% жалась к верху).
