# -*- coding: utf-8 -*-
"""engine — движок статических проверок скилла.

Раньше это был один main() на 806 строк, где все проверки читали общие
локальные переменные. Оттуда росли самые дорогие ложные тревоги: C6b брал
первый catch в файле, S5 резала строки регуляркой по всему исходнику
(RETRO 60). Проверка, у которой нет своей границы, рано или поздно судит
не тот кусок кода.

Здесь граница есть:
  ctx.py       — РАЗБОР. Один раз строит проекции исходника, склеенный CSS,
                 тела функций, NOTES. Проверки только читают готовое.
  registry.py  — РЕЕСТР. Проверка = функция над Ctx, объявленная декоратором
                 с кодами, которые она вправе печатать. Манифест checks.json
                 и реестр сверяются автоматически.
  cssglue.py   — склейка и ЛИНТ готового CSS.
  checks_*.py  — сами проверки, по доменам.
  report.py    — печать и вердикт с разделением доменов.

Точка входа — validate.py рядом.
"""
