# FIELDS — поля для «Измерений» Proteus

Поля из SQL не попадают в `data` автоматически. Каждое поле нужно вручную
добавить в панель «Измерения» с точным именем.

Правило синхрона: **имя в SQL == имя в «Измерениях» == ключ в `CFG.fields`**.
Любое расхождение — поле не дойдёт до графика (RETRO 17).

Типы в Proteus: `f(x)` — вычисляемое, `#` — число, `abc` — строка.

## Поля графика — виджет 1: тело, `pa-reports-body.chart.js` (чарт 788805)

Датасет **pa_body_v42** (id 169602; SQL внутри — шаблон v6: все разрезы
одним ответом + люди/отчётные фильтры полки/«кто смотрит»). 37 колонок =
36 v5.1 + **state_j** (новая, добавить в «Измерения» вручную); ov-секций
(lvl3/spec) больше нет — людские разрезы живут в чарте pa-who.

| Поле (точное имя) | Тип | Назначение |
|---|---|---|
| section | abc | секция: rep / grp / total — ВСЕ в одном ответе |
| grain | abc | грануляция: d/w/m/q |
| dashboard_id | # | id отчёта (rep) |
| group_key | abc | collection / owner (grp) |
| group_val | abc | значение группы (grp) |
| cut_key | abc | заглушка NULL (совместимость формы v5.1) |
| cut_val | abc | заглушка NULL |
| cohort_month | abc | заглушка NULL — когорты в своём датасете |
| cohort_size | # | заглушка 0 |
| dash_nm | abc | название отчёта |
| owner_login | abc | владелец отчёта |
| colls | abc (Array(String), JSON-строкой) | коллекции отчёта |
| published | # | признак публикации |
| certified | abc | кем сертифицирован (в UI больше НЕ рисуется) |
| created_dt | # (DATETIME, epoch) | дата создания отчёта |
| users, users_prev | # | пользователи за период / предыдущий |
| views, views_prev | # | просмотры за период / предыдущий |
| new_users, new_users_prev | # | новые пользователи |
| regular_users, regular_users_prev | # | постоянные (8+ раз) |
| sleeping_users | # | не заходили >30 дней |
| views_per_user | # | просмотров на пользователя |
| last_view_days | # | дней с последнего просмотра |
| freq_1 … freq_5 | # | корзины частоты «1»/«2–3»/«4–7»/«8–15»/«16+» |
| ts_k | abc (Array(Int64), JSON-строкой) | возраст бакета от max_date |
| ts_users, ts_new, ts_react | abc (Array(UInt64)) | пользователи/новые/вернувшиеся по бакетам |
| ts_views | abc (Array(Int64)) | просмотры по бакетам |
| state_j | abc (JSON-строкой, только total) | активные условия запроса: period/pub/act/exc/heads/lvl3/lvl4/stream/spec/adg/login/freq — рисуется чипами условий (без крестика: снимаются в своих чартах) |

«Вернувшиеся» (ts_react): не заходили последние **7 дней** (грануляция d)
или предыдущий бакет (w/m/q) — окно задаётся 4-м элементом GRAINS в
`.gen_cube5.py`.

Кросс-фильтр (эмит тела): `mode_param` (report/collection/owner — разрез
ПОСЛЕДНЕГО одиночного выбора) + `sel_f` (значение одиночного выбора).
`scope_param` в v5 НЕТ. Маску `mode_param/sel_f` читает ТОЛЬКО датасет
когорт: тело самовлияние не использует (отключено владельцем в UI) и не
нужно — все разрезы приходят одним ответом, смена вкладки клиентская.

С v5.1 тело и когорты — ещё и ПРИЁМНИКИ маски полки (колонки `period_param`
и `*_f`, см. «Виджет 3»): люди-условия фильтруют атрибуты (attrs), отчётные —
факт (evd). Эмит-колонки полки в SELECT приёмников НЕ выводятся — платформа
несёт их в маске кросс-фильтра (фильтр по ARRAY-колонке в SELECT роняет CH).

С v6 тело — ещё и ПРИЁМНИК ЛЮДСКОЙ ШИНЫ pa-who (`lvl3_f/lvl4_f/stream_f/
spec_f/adg_f/heads_f/login_f/freq_f`) и отдаёт `state_j`: JSON активных
условий в ответе total-строки, из него рисуются чипы условий. Динамика,
частота и «кто смотрит» из тела УШЛИ (чарт pa-who, виджет 6): ts-массивы
в ответе остаются — MAU-карточка читает их же.

### Виджет 2: закрепляемость — `pa-cohorts.chart.js` (чарт pa_cohorts)

Датасет **pa_coh_v4** (id 169589). 8 колонок:

| Поле (точное имя) | Тип | Назначение |
|---|---|---|
| section | abc | coh / rcoh / gcoh / ocoh |
| sel_key | abc | report / owner / collection / lvl3 / spec / … |
| sel_val | abc | значение выбора (id, логин, название) |
| sel_nm | abc | человекочитаемое имя выбора (имя отчёта) |
| cohort_month | abc (DATE, 'YYYY-MM-01') | месяц когорты |
| cohort_size | # | размер когорты |
| ages | abc (Array(Int64), JSON-строкой) | возрасты ступеней 1..11 |
| acts | abc (Array(UInt64), JSON-строкой) | активные в каждом возрасте |

Приёмник кросс-фильтра тела, сам НЕ эмитит. Выбор рисуется пилюлькой
в заголовке (единый стиль с чипами тела). Лимит строк 10 000.

## Поля графика — виджет 3: полка фильтров, `pa-shelf.chart.js` (чарт новый)

Датасет **pa_dicts** — НОВЫЙ виртуальный над `prod_proteus.proteus_adoption`
(SQL — файл 3 поставки 2026-09-19). Все секции справочника одной таблицей,
~23 000 строк (own 7858 + auth 4814 + rep ~6152 + adg 3374 + coll 1363 +
lvl3/lvl4/stream/spec ~100).

| Поле (точное имя) | Тип | Назначение |
|---|---|---|
| section | abc | секция: rep / coll / own / auth / lvl3 / lvl4 / stream / spec / adg |
| k | abc | ключ значения (у rep = dashboard_id строкой) |
| nm | abc | подпись для списка |
| parent | abc | блок lvl3 для lvl4-строк (дерево УС), иначе `''` |

Лимит строк чарта **30 000**. Порядок секций и сортировка списков — в JS
полки (`CFG.order`, сортировка по nm); от порядка строк в ответе виджет
не зависит.

Эмит полки — 15 колонок, все с operator `IN` (свитки несут `'0'/'1'`;
`value=[]` НЕ эмитится — пустой список роняет запрос; полный сброс =
`applyCrossFilter([])`): `period_param`, `rep_f`, `coll_f`, `own_f`,
`auth_f`, `pub_f`, `act_f`, `cert_f`, `lvl3_f`, `lvl4_f`, `stream_f`,
`spec_f`, `adg_f`, `heads_f`, `exc_f`.

Дефолты `pub_f/act_f/exc_f = '1'` зашиты в джине куба (как включённые
свитки макета): полка эмитит свиток ТОЛЬКО когда он отличается от дефолта.
Период — константы JS (30 дней/20 недель/12 месяцев/8 кварталов), в data
его нет; дефолт `period_param='d'` — тоже в джине.

## Поля графика — виджет 4: панель «Аудитория», `pa-audience.chart.js` (чарт новый)

Датасет **pa_aud_v1** — НОВЫЙ виртуальный над предагрегатами
`prod_proteus.pa_evd_day × pa_emp_attrs × pa_dash_meta` (SQL —
`pa-audience.data.sql`; джиня — та же, что у куба v6: period_param, свитки
и люди-фильтры полки, + sel-маска области mode_param/sel_f как у когорт).
Ответ ~до 400 строк: kpi-строка + разрезы lvl3/lvl4(дерево)/spec/stream/
adg(топ-40)/heads(2) + top-25 отчётов (только при людской области).

| Поле (точное имя) | Тип | Назначение |
|---|---|---|
| section | abc | 'kpi' (одна строка итогов) \| 'cut' (строки разрезов) |
| cut_key | abc | lvl3 / lvl4 / spec / stream / adg / heads / report (у kpi — `''`) |
| cut_val | abc | значение разреза (у report = dashboard_id строкой) |
| parent | abc | блок lvl3 для lvl4-строк (дерево УС), иначе `''` |
| nm | abc | подпись (название отчёта для report) |
| users | num | зрители за период |
| users_prev | num | зрители предыдущего периода (q — пусто) |
| views | num | просмотры |
| views_prev | num | просмотры за предыдущий период |
| new_users | num | первое касание в окне |
| regular_users | num | 8+ активных бакетов |
| once_users | num | ровно 1 бакет |

Все 12 полей добавляются в «Измерения» чарта (агрегаций в чарте нет —
строки уже агрегированы в SQL; JS-хелпер num() принимает и число, и строку).
Лимит строк **1000**. Порядок
разрезов и сортировка строк — в JS (`CFG.order`, сортировка по users).

Эмит панели (как у тела): `mode_param` (report \| cut:lvl3 \| cut:lvl4 \|
cut:spec \| cut:stream \| cut:head) + `sel_f` (одиночное значение).
Себя из скоупа исключить; влияет на `pa_cohorts`. Приёмники: эмита тела
(mode_param/sel_f), периода/свитков/людей полки.

## Поля графика — виджет 5: полоска настроек, `pa-strip.chart.js` (чарт новый, поставка первого листа 2.5)

Полей SQL НЕТ: период и свитки — константы в CFG, `data` игнорируется.
Датасет-носитель нужен только чтобы чарт существовал в Proteus.

| Поле (точное имя) | Тип | Назначение | Обязательно | Добавлено в «Измерения» |
|---|---|---|---|---|
| — (data не читается) | — | — | — | — |

Эмит (колонки кросс-фильтра, носители НЕ в SELECT приёмников — паттерн 59922):
`period_param` ('d'|'w'|'m'|'q', эмит при ≠d), `pub_f`/`act_f`/`exc_f` ('0',
эмит только при отклонении свитка от дефолта ВКЛ; value=[] не эмитится).
Сброс = `applyCrossFilter([])`. Приёмники: [тело 788805, pa_cohorts, pa-who].

## Поля графика — виджет 6: «Кто смотрит», `pa-who.chart.js` (чарт новый, первый лист 2.5)

Датасет `pa_who` (генератор `.gen_who.py` → `.pa_who.sql` = `pa-who.data.sql`).
Один ответ несёт 5 секций (section): list — поимённый список, ctx — контекст
групп области, freq — контекст корзин, total — счётчик, ts — динамика.
Лимит строк чарта **5000** (list-секция в SQL уже LIMIT 2000, adg LIMIT 100).

| Колонка | Тип | Alias в CFG.fields | В «Измерениях» |
|---|---|---|---|
| section | abc | section | [ ] |
| g | abc | g | [ ] |
| k | abc | k | [ ] |
| parent | abc | parent | [ ] |
| login | abc | login | [ ] |
| fio | abc (пусто до attrs) | fio | [ ] |
| lvl3 | abc | lvl3 | [ ] |
| lvl4 | abc | lvl4 | [ ] |
| spec | abc | spec | [ ] |
| stream | abc | stream | [ ] |
| exp | abc (пусто до attrs) | exp | [ ] |
| is_head | число | is_head | [ ] |
| days | число | days | [ ] |
| views | число | views | [ ] |
| last_dt | дата → epoch | last_dt | [ ] |
| bin | число 1–5 | bin | [ ] |
| users | число | users | [ ] |
| new_u | число | new_u | [ ] |
| react_u | число | react_u | [ ] |
| cnt | число | cnt | [ ] |

Эмит чарта (людская шина → тело 788805): `lvl3_f` / `lvl4_f` / `spec_f` /
`stream_f` / `adg_f` / `heads_f` (значение `1`) / `login_f` / `freq_f`;
сброс — `applyCrossFilter([])`. Приёмники: `mode_param`+`sel_f` (каталог
тела), `period_param` / `pub_f` / `act_f` / `exc_f` (полоска). Свою людскую
маску датасет НЕ читает (самовлияние чарта выключить).

## Что считается в JS, а что в SQL

| Расчёт | Где | Почему |
|---|---|---|
| views_per_user, regular_users | SQL (внешние выражения) | форма ответа 36 колонок под form_data чарта |
| доли, дельты, медианы когорт, MAU, freq-срез | JS | правило 2 скилла |
| фильтрация вкладок друг по другу | JS (meta + grp/ov-строки) | без похода в базу |
| дедуп-агрегация mix-выбора | JS (DEDUP) | без похода в базу |
| доли/бары/дельты/ИТОГО/наблюдения аудитории | JS (buildModel) | SQL — raw-агрегаты по (область × атрибут) |

## Статические значения (не из SQL)

| Значение | Где | Кто подтвердил |
|---|---|---|
| «данные: за вчера» в шапке | index.html freshness | NOTES §4 2026-09-17 (колонки max_ts в кубе нет) |

## Настроить РУКАМИ в Proteus (задачи пользователя)

Тело v6 (после ревью владельцем JS+SQL — папка Обновление соберётся отдельно):

- [ ] Заменить SQL в `pa_body_v42` (169602) на v6 (`pa-reports-body.data.sql`,
      проверка дефолта — SQL Lab по дефолт-рендеру `.pa6_body_default.sql`
      из `.gen_cube6.py`).
- [ ] Добавить в «Измерения» 169602 колонку `state_j` (abc) — единственная
      новая; остальные 36 на месте.
- [ ] JS чарта 788805 заменить на `pa-reports-body.chart.js` (v6).
- [ ] Скоуп 788805 не менять (только pa_cohorts); приёмники людской шины
      pa-who уже в джине v6.

Поставка 2026-09-19 (v5.1, применена владельцем):

- [ ] Создать датасет `pa_dicts` (SQL — файл 3), добавить 4 «Измерения»
      (section/k/nm/parent, все abc), убедиться в превью ~23К строк.
- [ ] Заменить SQL в `pa_body_v42` (169602) на файл 1 (проверка SQL Lab —
      файл 1a); ЗАМЕРИТЬ время входа (>12 с — стоп, вернуть v5).
- [ ] Заменить SQL в `pa_coh_v4` (169589) на файл 2 (проверка — файл 2a).
- [ ] Создать чарт полки: датасет pa_dicts, viz eCharts, JS — файл 4;
      «Измерения» — те же 4 поля; лимит строк 30 000.
- [ ] Поставить чарт полки на борд 60260 в левую колонку (слот shelf был
      снят 2026-09-18 — вернётся перетаскиванием чарта в левую колонку).
- [ ] Скоуп полки: влияет на 788805 + pa_cohorts, СЕБЯ исключить.
- [ ] Скоуп 788805 не менять (только pa_cohorts): полку в него НЕ добавлять —
      иначе клик по строке тела перечитает справочник 23К строк.
- [ ] После вставки: F5; дефолтные цифры тела/когорт УМЕНЬШАТСЯ против v5 —
      это ТЗ макета (включённые свитки: только опубликованные, только
      актуальные, без просмотров владельцев собственных отчётов).

Панель «Аудитория» (виджет 4) — после ревью владельцем JS+SQL (папка
Обновление соберётся отдельно):

- [ ] Создать датасет `pa_aud_v1` (SQL — `pa-audience.data.sql`), добавить
      12 «Измерений» по таблице виджета 4, убедиться в превью: ~до 400 строк
      при области «весь Proteus», период 30 дней.
- [ ] Создать чарт «Аудитория»: датасет pa_aud_v1, viz eCharts, JS —
      `pa-audience.chart.js`; лимит строк 1000.
- [ ] Поставить чарт на борд 60260 под телом «Отчёты и динамика» (объединённый
      экран «первая история + ЦА» из задачи владельца).
- [ ] Скоуп чарта: влияет на `pa_cohorts`, СЕБЯ исключить (как у тела);
      приёмник эмита тела mode_param/sel_f и фильтров полки.
- [ ] adg-строки клика не имеют (когорты cut:adg не считают) — тултип
      говорит об этом; когорты по AD — хвост NOTES §6.

«Кто смотрит» (виджет 6) — после ревью владельцем JS+SQL:

- [ ] Создать датасет `pa_who` (SQL — `pa-who.data.sql`), добавить все 20
      «Измерений» по таблице виджета 6, убедиться в превью: ~30 строк ts
      + список + контексты (период 30 дней).
- [ ] Создать чарт «Кто смотрит»: датасет pa_who, viz eCharts, JS —
      `pa-who.chart.js`; лимит строк 5000.
- [ ] Поставить чарт на борд 60260 в правую колонку первого листа
      (слот правой панели макета 2.5).
- [ ] Самовлияние чарта ВЫКЛЮЧИТЬ (свою людскую маску не читает — гашение
      списка локальное); скоуп: влияет на 788805 (+ куб v6), себя исключить.
- [ ] Приёмники: mode_param/sel_f (клик каталога тела), period_param/
      pub_f/act_f/exc_f (полоска).
