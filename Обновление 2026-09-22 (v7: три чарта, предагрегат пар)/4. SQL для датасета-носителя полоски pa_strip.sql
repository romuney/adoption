-- pa_strip — датасет-носитель чарта «Полоска настроек».
-- Полоска данных не читает (период и свитки — константы в её JS), датасет нужен
-- только чтобы чарт существовал в Proteus. Раньше носителем был pa_dicts —
-- справочник на 23 тыс. строк из UNION восьми плеч над старым фактом
-- proteus_adoption: он исполнялся целиком на каждом входе в борд ради одной строки.
SELECT toUInt8(1) AS one
